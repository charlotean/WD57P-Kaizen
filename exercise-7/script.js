/* let pets = ["dogs", "cats", "birds", "fishes"];

let newIncludes = pets.includes("cats");
console.log(newIncludes);

function checkPets(word) {
  if (pets.includes(word)) {
    return word;
  } else {
    return word + " not found.";
  }
}

let userInput = prompt("Give me a pet name");
console.log(checkPets(userInput)); */